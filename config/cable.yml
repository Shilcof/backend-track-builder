development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  url: redis://track-builder.herokuapp.com:6379
  channel_prefix: backend_track_builder_production
